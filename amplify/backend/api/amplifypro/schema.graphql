type HomePageImages @model @auth(rules: [{allow: public}]) {
  id: ID!
  HeroBannerImage: AWSURL
}

type SubscribeNow @model @auth(rules: [{allow: public}]) {
  id: ID!
  SubscriptionEmailId: String
}

type ContactUsCreateForm @model @auth(rules: [{allow: public}]) {
  id: ID!
  Name: String
  EmailId: String
  Message: String
} 